// This file is kept for backwards compatibility but the
// real product creation now goes through src/api/products.ts -> createProduct()

export { createProduct as createProductAPI } from './products';
